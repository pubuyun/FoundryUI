# Loads

Load nodes create the initial typed payloads.

Uploaded browser files are embedded in the workflow/session document for backend execution. The backend validates and normalizes the file before producing artifacts.

Ligands are standardized as PDB. Single ligand uploads are renamed to distinct residue names; batch ligand uploads keep their original residue names.

# Saves

Save nodes copy selected intermediate artifacts into run-relative save folders.

Saved outputs are available from the run artifact list and can be included in the run archive.

Use save nodes when a workflow has several intermediate branches but only some outputs should be considered final.

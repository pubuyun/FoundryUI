# Installtion Script

FoundryUI production setup is intentionally separate from this documentation site.

Use the repository installation script for the application runtime. It clones the project, installs backend and frontend environments, runs model setup, and configures the production runner.

The docsify site in `docs/` is static HTML and Markdown. It does not need to be installed as part of normal FoundryUI operation.

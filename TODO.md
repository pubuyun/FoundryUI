- Saves bar should also show sequence saved and ligand saved.
- create a folder where I can put some fuiworkflow in (The workflow file that saved by the frontend), and add a dropdown menu on Load button, where options of presets of fuiworkflows can be selected.
- Add a download icon beside "Archive Download", to indicate it can be downloaded.
- Delete these progress bars:"Validate typed graph
  Package Ryvencore workflow
  Queue Foundry task
  Stream model and score results"
- Remove the left bar of adding nodes, only keep the popup menu when right click.
- optimize the popup menu on right click. It should appear on the top layer, not being blcked by the Status bar. Also it should have different colors indicating differnet nodes. And if the space is not enough to display it, automatically turn to two columns.

Add options: select all and clear all in 3d viewer and selectors of node Select Atoms.
REFACTOR UI:
Add feature: if any node(including non-heavy nodes) is cached, make them display a green CACHED(by adding it in the position below MANUAL sign)
The Submit button of Filter by score should below the drop-down selector

# Workflow Presets

Put saved `.fuiworkflow` files in this directory and register them in `presets.json`.

Example:

```json
{
  "presets": [
    {
      "label": "RF3 Ligand Design",
      "file": "rf3-ligand-design.fuiworkflow"
    }
  ]
}
```

The frontend serves these files from `/workflows/`.
